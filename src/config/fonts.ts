/**
 * Stock template: system-ui only (offline-friendly builds).
 * When forking: docs/FONTS.md — loader here, appFont.variable on <body> in layout.tsx,
 * and the same family name literally in globals.css --app-font (never var(--font-*) on :root).
 */
export const appFont = {
  variable: "",
}
